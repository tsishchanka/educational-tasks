import { ReactNode } from 'react';
export declare type ToastPortalProps = {
    children: ReactNode;
};
declare const ToastPortal: ({ children }: ToastPortalProps) => JSX.Element;
export default ToastPortal;
